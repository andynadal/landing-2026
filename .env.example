# Ghost CMS Configuration
# These environment variables should NOT be exposed to the client
# They are used server-side only for SSR/SSG

# Ghost Content API URL (e.g., https://your-ghost-site.com)
GHOST_CONTENT_URL=

# Ghost Content API Key
GHOST_CONTENT_API_KEY=
